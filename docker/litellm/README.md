# LiteLLM

This directory contains configuration and documentation for the LiteLLM gateway.

LiteLLM provides a single OpenAI-compatible API endpoint for local and cloud language models.

Initially, this gateway will route requests to the local Ollama instance.
