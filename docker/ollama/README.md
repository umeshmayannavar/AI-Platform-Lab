# Ollama

This directory is reserved for Ollama-specific container configuration such as model bootstrap scripts, custom Modelfiles, or runtime notes.

The initial Docker Compose foundation uses the upstream `ollama/ollama` image and persists model data in the `ollama-data` named volume.
