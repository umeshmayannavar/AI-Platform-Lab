# Open WebUI

This directory is reserved for future Open WebUI configuration.

The Docker Compose service is intentionally placed behind the `webui` profile so it can be enabled when the platform needs a browser-based interface for Ollama.
