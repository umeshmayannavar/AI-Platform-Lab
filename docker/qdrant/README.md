# Qdrant

This directory is reserved for future Qdrant collection configuration, snapshots, and vector database operational notes.

The Docker Compose service is intentionally placed behind the `vector-db` profile so it can be enabled when RAG or vector search workloads are introduced.
