# Kubernetes

Kubernetes is an open-source container orchestration platform.

It automates deployment.

It automates scaling.

It automates networking.

It automates storage.

It runs workloads across clusters.